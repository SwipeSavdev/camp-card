# ============================================================================
# Camp Card - AWS Environment Configuration
# ============================================================================
# Production deployment on EC2: 18.190.69.205
# Domain: campcardapp.org
# ============================================================================

# ==========================================================================
# Database Configuration (AWS RDS)
# ==========================================================================
DB_HOST=camp-card-db.cn00u2kgkr3j.us-east-2.rds.amazonaws.com
DB_PORT=5432
DB_NAME=campcard
DB_USERNAME=campcard_app
DB_PASSWORD=CampCardApp2024Secure

# ==========================================================================
# Redis Configuration
# ==========================================================================
REDIS_PASSWORD=campcard123

# ==========================================================================
# JWT Configuration
# ==========================================================================
JWT_SECRET=bsa-camp-card-super-secret-jwt-key-2025-that-is-very-long-and-secure

# ==========================================================================
# Web Portal Configuration (admin.campcardapp.org)
# ==========================================================================
PUBLIC_API_URL=https://api.campcardapp.org/api/v1
NEXTAUTH_URL=https://admin.campcardapp.org
NEXTAUTH_SECRET=bsa-campcard-nextauth-secret-key-2025-very-long

# ==========================================================================
# Email Links Configuration
# ==========================================================================
# Production URLs for campcardapp.org domain
CAMPCARD_BASE_URL=https://api.campcardapp.org
# Web Portal URL for password reset and email verification links
CAMPCARD_WEB_PORTAL_URL=https://admin.campcardapp.org

# Legacy URLs (bsa.swipesavvy.com) - keep for transition period:
# PUBLIC_API_URL=https://bsa.swipesavvy.com/api/v1
# NEXTAUTH_URL=https://bsa.swipesavvy.com
# CAMPCARD_BASE_URL=https://bsa.swipesavvy.com
# CAMPCARD_WEB_PORTAL_URL=https://bsa.swipesavvy.com

# ==========================================================================
# Email Configuration (AWS SES)
# ==========================================================================
# SES SMTP credentials - need to verify campcardapp.org domain in SES
SMTP_HOST=email-smtp.us-east-2.amazonaws.com
SMTP_PORT=587
SMTP_USERNAME=AKIA4P7NVGN7DPQTMHCX
SMTP_PASSWORD=BFyY1A5LvRNm4UultlbOh2zhq0l2mNsBt7VYXrqCVhvV

# ==========================================================================
# Optional: Kafka Configuration
# ==========================================================================
# Kafka is configured internally
# No external configuration needed unless using external Kafka
